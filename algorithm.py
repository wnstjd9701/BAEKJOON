# 파이썬 알고리즘에서 사용 되는 함수 or 로직 모음
from collections import deque

from collections import defaultdict
d = defaultdict(int) # Int형 defaultdict

num = ord('A') # 알파벳을 아스키 코드 숫자로 반환

a = 'a'
a.isupper() # 대문자인지 확인
a.islower() # 소문자인지 확인